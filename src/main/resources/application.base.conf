system {

  name = "poc_manager"

  server {
    port = 8082
    baseUrl = ""
    swaggerPath = "src/main/scala/com/ubirch/resources/swagger-ui/"
  }

  metrics {
    prometheus {
      port = 4321
    }
  }

  executionContext {
    threadPoolSize = 5
  }

  services {
    dataSchemaGroupMap = """{"ub_vac_app": "DATA_SCHEMA_certification-bvdw-certificate", "ub_test_app": "DATA_SCHEMA_certification-bvdw-certificate"}"""
    dataSchemaGroupIdMap = """{"DATA_SCHEMA_certification-bvdw-certificate": "0aa073cb-89de-49c5-867a-01ae22cd6fb9", "DATA_SCHEMA_certification-corona-test": "99532141-0835-45a1-ba11-81f10d728d62"}"""
    endpointMap: """{"ub_vac_app": "/api/v1/anchor", "ub_test_app": "/api/v1/anchor", "ub_test_api": "/api/v1/x509/anchor", "bmg_vac_app": "/api/certify/v2/issue/hash", "bmg_vac_api": "/api/certify/v2/issue", "ub_cust_app": "/api/v1/anchor" }"""

    # urls and tokens for testing
    certifyApiURL = "https://api.certify.dev.ubirch.com/api/identity/v1/register"
    certifyApiToken = "certifyApiToken"
    goClientURL = "not reachable from outside"
    goClientToken = "goClientToken"
    thingApiURLCreateDevice = "https://api.console.dev.ubirch.com/ubirch-web-ui/api/v1/devices/create?with_api_info=true"
    thingApiURLGetInfo = "https://api.console.dev.ubirch.com/ubirch-web-ui/api/v1/devices/api-config"
    pocCreationInterval = 2
  }

  aesEncryption {
    //this config (file) is only used for local and unit testing
    secretKey = "NHQ3dzl6JEMmRilKQE5jUmZValhuMnI1dTh4L0ElRCo="
  }
}

keycloak-certify {
  server {
    url = "http://localhost:8080/auth"
    realm = "master"
    # this config (file) is only used for local and unit testing
    username = admin
    password = admin
    clientId = "admin-cli"
  }
  realm = "certify-realm"
  polling {
    interval = 30
  }
  client {
    //this config (file) is only used for local and unit testing
    config = """{ "realm": "certify-realm", "auth-server-url": "http://localhost:8080/auth", "ssl-required": "external", "resource": "ubirch-2.0-user-access-local", "credentials": { "secret": "ca942e9b-8336-43a3-bd22-adcaf7e5222f" }, "confidential-port": 0 }"""
    adminUsername = client-admin
    adminPassword = client-admin
  }
  tokenVerification {
    configURL = "http://localhost:8080/auth/realms/certify-realm/.well-known/openid-configuration"
    kid: "6dMHOUfu7v6howP2WH5bkp-j9UgUYdyEQbWJp8cb8IY"
  }
}

keycloak-device {
  server {
    url = "http://localhost:8081/auth"
    realm = "master"
    //this config (file) is only used for local testing
    username = admin
    password = admin
    clientId = "admin-cli"
  }
  realm = "device-realm"
  tokenVerification {
    configURL = "http://localhost:8081/auth/realms/device-realm/.well-known/openid-configuration"
    kid: "tgxjDoFtQP7tzzO6byck4X8vsFRaM5EVz0N66O9CSTg"
  }
}

database {
  dataSourceClassName = org.postgresql.ds.PGSimpleDataSource
  dataSource.user = postgres
  dataSource.password = postgres
  dataSource.databaseName = postgres
  dataSource.portNumber = 5432
  dataSource.serverName = localhost
  connectionTimeout = 30000
}